// GENERATED from `spec/webgpu.idl` via `wgpu.api`. Edit neither.

package wgpu;

/** `BlendOperation` from the WebGPU IDL: its values, in its order. **/
enum abstract BlendOperation(Int) from Int to Int {
	var Add = 0;
	var Subtract = 1;
	var ReverseSubtract = 2;
	var Min = 3;
	var Max = 4;
}
