// GENERATED from `spec/webgpu.idl` via `wgpu.api`. Edit neither.

package wgpu;

/** `CullMode` from the WebGPU IDL: its values, in its order. **/
enum abstract CullMode(Int) from Int to Int {
	var None = 0;
	var Front = 1;
	var Back = 2;
}
