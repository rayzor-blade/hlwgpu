// GENERATED from `spec/webgpu.idl` via `wgpu.api`. Edit neither.

package wgpu;

/** `VertexStepMode` from the WebGPU IDL: its values, in its order. **/
enum abstract VertexStepMode(Int) from Int to Int {
	var Vertex = 0;
	var Instance = 1;
}
