// GENERATED from `spec/webgpu.idl` via `wgpu.api`. Edit neither.

package wgpu;

/** `FrontFace` from the WebGPU IDL: its values, in its order. **/
enum abstract FrontFace(Int) from Int to Int {
	var Ccw = 0;
	var Cw = 1;
}
